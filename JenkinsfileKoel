pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'ls -la'
                sh 'mvn clean compile'
            }
        }

        stage('Test') {
            steps {
                sh 'mvn test -DincludeTags=smoke'
            }
        }
    }
}